import styled from 'styled-components';

const Header = styled.header`
    background-color: rgb(251, 63, 92);
    padding: 20px 0 15px;
    position: fixed;
    top: 0;
    left: 0;
    right: 0;
    z-index: 999;
`

export const S = {
    Header
}